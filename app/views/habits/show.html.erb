<h1 class="text-2xl font-bold mb-4"><%= @habit.name %></h1>

<p class="mb-4 text-gray-700">
  <strong>Description:</strong>
  <%= @habit.description.presence || "No description provided." %>
</p>

<div class="space-x-4">
  <%= link_to 'Edit', edit_habit_path(@habit), class: "text-blue-600 hover:underline" %>
  <%= link_to 'Back', habits_path, class: "text-gray-600 hover:underline" %>
</div>
